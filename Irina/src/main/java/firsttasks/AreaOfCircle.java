package firsttasks;
/*
Compute the area of a circle and display it
 */

public class AreaOfCircle {
    public static void main(String[] args) {
        double radius = 15.35;
        double circleArea = Math.PI * (radius * radius);
        System.out.println(circleArea);
    }
}